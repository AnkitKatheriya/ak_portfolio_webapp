export interface IHomeContainerProps {
    children: React.ReactNode,
}

export interface IHomeNavBarContainer {
    children: React.ReactNode,
}

export interface IHomeLogo {
    children: React.ReactNode,
}

export interface IHomeNavbar {
    children: React.ReactNode,
}

export interface IHomeNavBarItems {
    children: React.ReactNode,
}

export interface IHomeNavBarItem {
    children: React.ReactNode,
}